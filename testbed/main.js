import Button from './button.vue';
import Player from './player.ts';

export default function main(value) {
  const { test } = value;

  console.log(test);
  console.log(Button);
  console.log(Player);
}